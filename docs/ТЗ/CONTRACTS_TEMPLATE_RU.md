# Техническое задание — Создание договоров по шаблону

Версия: 1.0 (черновик)  
Дата: 2026-01-22

## 1. Цель и результат

Пользователь бота выбирает тип договора, вводит необходимые данные, система подставляет их в шаблон соответствующего языка и темы из базы и сохраняет итоговый текст в «Мои договоры». В будущем добавляется генерация PDF.

Ожидаемый результат:
- договор формируется строго из шаблона + введенных данных;
- соблюдаются шариатские ограничения;
- договор сохраняется как черновик/подтвержденный и доступен в списке «Мои договоры».

## 2. Термины

- **Шаблон договора** — текстовый документ с плейсхолдерами (например, `{{field_name}}`), хранящийся в базе и привязанный к `topic` и языку.
- **Договор** — экземпляр, созданный пользователем из шаблона.
- **Тема (topic)** — ключ шаблона в формате `contracts.{category}.{template}`.

## 3. UX и структура кнопок

### 3.1 Кнопка «Создать договор»

При нажатии показывается список типов договоров:
- 💸 Карды-хасан (займ без процентов)
- 🏠 Аренда / Иджара
- 🚚 Предоплата — Саляма
- 🛠 Изготовление — Истисна’
- 💼 Купля-продажа / Бай’
- 👥 Партнёрство / Мушарака
- 📊 Инвестиция / Мудараба
- 🎁 Дарение / Хиба
- 📦 Хранение / Аманат
- 🛡 Поручительство / Кафаля
- ⚖️ Мирное соглашение / Сульх
- ↩️ Назад

Исключения: «Никах» и «Наследство/Васийя» находятся в других ветках и не дублируются в этом списке.

### 3.2 Логика взаимодействия

После выбора договора запускается пошаговый сбор данных:
- сбор по шагам, с проверкой корректности на каждом шаге;
- на каждом шаге есть кнопка «Отменить»;
- после ввода всех данных показывается кнопка «Сформировать договор»;
- после формирования показывается предпросмотр текста и кнопки действий.

### 3.3 Смена языка интерфейса

Требования:
- пункт «Язык» доступен всегда из главного меню и из «Профиль/Настройки»;
- меню смены языка показывается независимо от текущего языка (в т.ч. `ar`);
- язык можно сменить в любой момент без повторной регистрации;
- выбранный язык сохраняется в профиле пользователя и применяется ко всем экранам бота;
- если для выбранного языка нет шаблонов, показывается сообщение «Шаблон недоступен» и предлагается выбрать другой язык.

## 4. Наборы полей (минимум)

### 4.1 Карды-хасан (Qard)
- Имя дающего
- Имя получателя
- Сумма
- Срок возврата (дата или текст)
- Залог (да/нет → если да, описание)
- Доп. условия (опционально)

Автопроверка: если пользователь вводит проценты/выгоду → сообщение:
> ⚠️ Проценты, выгода или риба запрещены в шариате. Удалите пункт.

### 4.2 Аренда / Иджара
- Арендодатель
- Арендатор
- Объект аренды
- Срок
- Цена
- Ответственность при порче (кнопки):
  1) По вине арендатора
  2) По договорённости

### 4.3 Саляма (Salam)
- Покупатель
- Поставщик
- Что будет поставлено (описание)
- Срок поставки
- Стоимость (фиксированная)
- Место получения

Автопроверка: если цена указана как «по ситуации/на момент» → сообщение:
> ⚠️ Цена должна быть фиксирована заранее по шариату.

### 4.4 Истисна’ (Istisna)
- Заказчик
- Исполнитель
- Что изготовить
- Срок
- Материалы: чьи? (кнопки: заказчика / исполнителя)
- Цена

### 4.5 Купля-продажа / Бай’
- Продавец
- Покупатель
- Товар
- Состояние товара (новый/б/у)
- Цена
- Когда производится оплата (до/после/частями)

### 4.6 Мушарака
- Участник 1: вклад
- Участник 2: вклад
- Распределение прибыли (в %)
- Распределение убытков (по доле капитала)

### 4.7 Мудараба
- Инвестор (вкладывает деньги)
- Управляющий (работает)
- Сумма капитала
- Доля прибыли: инвестор _% / управляющий _%

Автопроверка: если пытается гарантировать прибыль → сообщение:
> ⚠️ В шариате прибыль не гарантируется, только распределяется.

### 4.8 Дарение / Хиба
- Даритель
- Получатель
- Что дарится
- Есть ли условие возврата (если да → ошибка, кроме исключения: отец детям)

### 4.9 Аманат / Хранение
- Владелец имущества
- Хранитель
- Имущество (описание)
- Срок хранения
- Ответственность хранителя: только при доказанной вине (по умолчанию)

### 4.10 Кафаля
- Поручитель
- За кого
- Обязательство
- Срок поручительства

### 4.11 Сульх
- Сторона А
- Сторона Б
- Суть спора
- Предлагаемое решение
- Стороны отказываются от претензий? (Да/нет)

## 5. Шаблоны, язык и вставка данных

### 5.1 Выбор шаблона

Для каждого типа договора задан `topic` и категория:
- Карды-хасан → `contracts.finance.qard`
- Иджара → `contracts.exchange.ijara`
- Саляма → `contracts.exchange.salam`
- Истисна’ → `contracts.exchange.istisna`
- Бай’ → `contracts.exchange.bay`
- Мушарака → `contracts.partnership.musharaka`
- Мудараба → `contracts.partnership.mudaraba`
- Хиба → `contracts.gratis.hiba`
- Аманат → `contracts.settlement.amana`
- Кафаля → `contracts.finance.kafala`
- Сульх → `contracts.settlement.sulh`

### 5.2 Язык

Алгоритм выбора файла шаблона:
1) язык пользователя (например, `ru`, `en`, `ar`);
2) fallback на дефолтный язык проекта;
3) если нет шаблонов — выводим ошибку «Шаблон недоступен» и предлагаем вернуться.

### 5.3 Формат шаблона и подстановки

Шаблон хранится в базе как текстовый документ (в будущем — DOCX/PDF с текстовыми тегами).
Плейсхолдеры: `{{field_key}}`.

Правила подстановки:
- обязательные поля без значения → блокировка «Сформировать договор»;
- опциональные поля без значения → строка удаляется или заменяется на пустое значение;
- дата/сумма форматируются по локали пользователя.

Пример:
```
Давший займ: {{lender_name}}
Получивший: {{borrower_name}}
Сумма: {{amount}}
Срок возврата: {{due_date}}
```

## 6. Шариат-фильтр и проверки

Встроенные проверки:
- проценты/выгода/риба (в т.ч. «%», «сверху») → ошибка, запрет продолжения;
- неясная цена («как получится», «потом решим», «на момент») → требовать фиксацию;
- харам-товары (алкоголь, наркотики, оружие массового поражения и т.п.) → отказ;
- неизвестные условия («потом договоримся») → требовать уточнение.

Общее сообщение:
> ⚠️ Это условие нарушает шариат. Измените или удалите пункт, чтобы продолжить.

## 7. Сохранение и «Мои договоры»

### 7.1 Таблица `contracts`

Минимальные поля:
- `id`
- `user_id`
- `type` (enum)
- `template_topic`
- `language`
- `data` (json, все поля договора)
- `rendered_text` (текст договора)
- `status` (draft/confirmed/sent_to_party/party_approved/party_changes_requested/signed/sent_to_scholar/scholar_send_failed/sent)
- `invite_code` (код приглашения к договору, nullable)
- `responsible_admin_id` (ответственный админ, nullable)
- `scholar_id`, `scholar_name`, `scholar_contact` (назначенный учёный, nullable)
- `created_at`
- `updated_at`

### 7.2 Интеграция с «Мои договоры»

После подтверждения договор сохраняется как документ пользователя:
- виден в разделе «Мои договоры»;
- можно скачать как PDF.



### 7.3 Согласование второй стороной (не криптоподпись)

После просмотра договора владелец может отправить его контрагенту:
- режим выбора получателя:
  - ввод `@username`/Telegram ID;
  - или кнопка «Выбрать контакт» (request_user).
- если контрагент не зарегистрирован в боте:
  - отправляется ссылка на регистрацию в боте;
  - после регистрации контрагенту автоматически отправляется договор на согласование/подписание.
- если контрагент зарегистрирован:
  - ему отправляется текст/TXT договора + кнопки **«Подтвердить / Нужны правки / Подписать договор»**.
  - в поле «Выберите действия» у контрагента доступна кнопка **«Подписать договор»**.

Дополнительно:
- договор сохраняется **у обоих контрагентов** в «Мои договоры»;
- второй контрагент имеет доступ **только на просмотр** (без редактирования полей);
- хранится и проверяется `recipient_id`; подтверждение/правки/подпись доступны только пользователю с этим Telegram ID.

действия контрагента:
- **Подтвердить** → `status="party_approved"`, `data.party_status="approved"`;
- **Нужны правки** → `status="party_changes_requested"` + `data.party_comment`;
- **Подписать договор** → `status="signed"`, `data.party_status="signed"`.

Примечание: это **не криптографическая подпись**, а обычное согласование в боте.

### 7.4 Отправка учёному (опционально)

В меню договора есть кнопка «Отправить учёному»:
- бот формирует TXT+PDF;
- отправляет в группу/канал учёных;
- `status` = `sent_to_scholar` или `scholar_send_failed`;
- создаётся Work Item (`topic="contracts"`, `kind="scholar_request"`).

### 7.5 Передача дела в суд

В меню «Выберите действия» для договора добавляется кнопка **«Передать дело в суд»**.
При нажатии:
- запускается процедура подачи в суд по этому делу (как в ветке судов);
- создаётся судебное дело на основе данных договора;
- пользователю показывается стандартный сценарий подачи/сбора данных суда.

## 8. Итоговый экран (после формирования)

Бот выводит:
- 📄 текст договора (одним сообщением);
- кнопки:
  - ✔️ Подтвердить и сохранить
  - ✏️ Изменить данные
  - 📤 Отправить другой стороне
  - ⬇️ Скачать текст PDF.
  - 🕌 Отправить учёному (реализовано, создаётся Work Item)

## 9. Автоподбор типа договора (опционально, P1)

Алгоритм:
1) Что хотите оформить? (семья/деньги/покупка/работа/аренда/хранение/дарение)
2) Есть ли деньги? (да → займ/купля/аренда/инвестиция; нет → дарение/хранение)
3) Есть ли товар?
   - есть сейчас → купля-продажа
   - будет позже, но деньги сейчас → Саляма
   - изготовят под заказ → Истисна’
4) Для бизнеса? → Мудараба / Мушарака
5) Подходит договор: {название}. Создать? (Да/Нет/Изменить ответы)

## 10. Критерии приемки

- Пользователь может выбрать тип договора, заполнить поля и получить сформированный текст.
- Текст формируется из шаблона соответствующего `topic` и языка.
- Нарушения шариатских ограничений блокируются.
- Договор сохраняется и отображается в «Мои договоры».
- «Отменить» доступно на каждом шаге сбора данных.
- Пользователь может сменить язык интерфейса через меню «Язык» из главного меню или «Профиль/Настройки», даже если зарегистрирован на `ar`.
  
 
## 11. Админ-панель (договоры)

Добавлен раздел «Договоры»:
- список договоров с фильтром по `status`;
- карточка договора (text, owner, counterparty, status);
- назначение ответственного админа;
- назначение учёного + обновление `status`.

В «Заданиях» появляются Work Items типа `contracts/scholar_request`.
ывапап